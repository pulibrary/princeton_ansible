---
- name: provision a vagrant box to mimic a production environment as closely as possible.
  hosts: vagrant
  #user: vagrant
  remote_user: pulsys
  #gather_facts: true
  vars:
  become: true
  vars_files:
    - site_vars.yml

  roles:
    - role: pulibrary.graylog2

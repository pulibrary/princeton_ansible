---
- hosts: matomo
  remote_user: pulsys
  become: true
  vars_files:
    - site_vars.yml
    - group_vars/matomo/matomo.yml
    - group_vars/matomo/vault.yml
  roles:
    - {role: pulibrary.mariadb, when: db_host == 'localhost'}
    - role: pulibrary.apache2
    - role: pulibrary.php
    - role: pulibrary.matomo

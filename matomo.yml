---
- hosts: dss
  remote_user: pulsys
  become: true
  vars_files:
    - site_vars.yml
  roles:
    - {role: pulibrary.mariadb, when: db_host == 'localhost'}
    - role: pulibrary.apache2
    - role: pulibrary.php
    - role: pulibrary.matomo

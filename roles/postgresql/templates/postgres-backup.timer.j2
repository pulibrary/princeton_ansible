[Unit]
Description=Scheduled rotated PostgreSQL backups

[Timer]
OnCalendar={{ postgres_backup_on_calendar }}
RandomizedDelaySec={{ postgres_backup_randomized_delay }}
# Catch up after a reboot or maintenance window that spanned the schedule.
Persistent=true
Unit={{ postgres_backup_service_name }}

[Install]
WantedBy=timers.target

---
# handlers file for roles/bind9
- name: Restart bind
  ansible.builtin.service:
    name: "{{ bind9_service_name }}"
    state: restarted
  become: true
  when:
    - not ansible_check_mode
    - running_on_server | default(true)

---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: check for bind9
    package:
      name: bind9
      state: present
    check_mode: true
    register: pkg_status

  - name: test that bind9 is installed
    assert:
      that:
        - not pkg_status.changed

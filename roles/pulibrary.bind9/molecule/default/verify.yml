---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: install dnsutlis
      package:
        name: ["bind9", "dnsutils"]
        state: present

    - name: test resolving www.google.com
      command: nslookup www.google.com 127.0.0.1

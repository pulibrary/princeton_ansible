---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: test for ruby app dir
    ansible.builtin.stat:
      path: "{{ item }}"
    loop:
      - "/opt/ruby_app"
    register: ruby_app
    failed_when:
      - not ruby_app.stat.exists
      - not ruby_app.stat.isdir

---
# tasks file for roles/friends_of_pul
- name: friends_of_pul | ensure memory limit is set in apache2 php.ini
  ansible.builtin.lineinfile:
    dest: "/etc/php/{{ php_version }}/apache2/php.ini"
    regexp: "memory_limit ="
    line: "memory_limit = 512M"

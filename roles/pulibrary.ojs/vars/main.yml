---
# vars file for roles/pulibrary.ojs
app_db_host: "{{ db_host | default('localhost') }}"
app_db_user: "{{ ojs_db_user }}"
app_db_password: "{{ ojs_db_password }}"
app_db_port: "{{ ojs_db_port }}"

application_dbuser_name: "{{ ojs_db_user }}"
application_dbuser_password: "{{ ojs_db_password }}"
application_db_name: "{{ ojs_db_name }}"
application_dbuser_role_attr_flag: "SUPERUSER"

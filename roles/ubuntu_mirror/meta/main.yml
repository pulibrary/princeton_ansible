---
galaxy_info:
  role_name: ubuntu_mirror
  company: Princeton University Library
  description: Ubuntu apt archive mirror
  author: pulibrary

  license: MIT

  min_ansible_version: 2.12

  platforms:
    - name: FreeBSD
      versions:
        - 15
dependencies:
  # uncomment the line below and remove the `[]` above in your role
  - role: pulmirror

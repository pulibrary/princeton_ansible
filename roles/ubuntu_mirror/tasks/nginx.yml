---
- name: Ubuntu mirror | install nginx location drop-in
  ansible.builtin.template:
    src: ubuntu-locations.conf.j2
    dest: "{{ ubuntu_mirror_nginx_dropin }}"
    owner: root
    group: wheel
    mode: "0644"
  notify: reload nginx

---
# handlers file for roles/opkssh
- name: restart ssh
  ansible.builtin.systemd:
    name: "{{ opkssh_os_packages[opkssh_os_type].ssh_service }}"
    state: restarted
    daemon_reload: true
  when: opkssh_restart_ssh

---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Test for nginx build files
      ansible.builtin.stat:
        path: "{{ item }}"
      loop:
        - "/opt/nginx"
        - "/opt/nginx/sbin/nginx"
        - "/opt/nginx/logs"

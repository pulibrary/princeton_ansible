- name: Converge
  hosts: all
  vars:
    - run_not_in_container: false
    - force_pas_deploy: false
  vars_files:
    - ../../defaults/main.yml
  roles:
    - role: pulibrary.mariadbserver
    - role: pulibrary.pas     # I am adding this here instead of a dependency so it does not run every time we run this role
    - role: pulibrary.pas_code

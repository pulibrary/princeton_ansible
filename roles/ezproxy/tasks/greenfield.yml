---
- name: Ezproxy | greenfield config.txt
  ansible.builtin.template:
    src: "config.txt.j2"
    dest: "/var/local/ezproxy/config.txt"
    owner: "{{ deploy_user }}"
    group: "{{ deploy_user }}"
    mode: "0644"
  when:
    - running_on_server

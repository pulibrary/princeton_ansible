---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
    - rails_app_name: 'figgy'
  become: true
  pre_tasks:
    - name: update cache
      apt:
        update_cache: true
  gather_facts: true
  tasks:
    - name: "Include redis"
      include_role:
        name: "redis"

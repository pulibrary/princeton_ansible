---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
    - solr_znode: new_znode
    # TODO: Try without this zookeeper_hosts var
    - zookeeper_hosts:
      - host:  localhost
        id: 1
        ip: 127.0.0.1
  roles:
    - role: pulibrary.solrcloud

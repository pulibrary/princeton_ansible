---
- name: Converge
  hosts: all
  vars:
    # TODO: Try without this zookeeper_hosts var
    - running_on_server: false
    - zookeeper_hosts:
      - host:  localhost
        id: 1
        ip: 127.0.0.1
    - zookeeper_hosts_solr_path: new_znode
    - zookeeper_hosts: localhost:2181
    - zk_host_setting: "{{ zk_host }}"
  roles:
    - role: pulibrary.solrcloud

---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
    - zookeeper_hosts: "all"
    - lib_zk1_host_name: "instance"
  roles:
    - role: pulibrary.zookeeper
    - role: pulibrary.solrcloud

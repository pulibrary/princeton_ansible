---
# handlers file for roles/solrcloud
- name: Restart solr
  ansible.builtin.systemd:
    name: solr
    state: restarted
    daemon_reload: true
  ignore_errors: true
  when: service_solrcloud_status is defined and service_solrcloud_status is not failed

- name: 'pul_nomad_datadog | add the application environment variables'
  ansible.builtin.shell:
    cmd: 'nomad var put -force nomad/jobs/datadog DATADOG_KEY={{ vault_datadog_key }}'
  environment:
    NOMAD_TOKEN: '{{ pul_nomad_management_token }}'
  run_once: true

---
- name: prepare
  hosts: all
  tasks:
    - name: prepare | install iproute
      ansible.builtin.apt:
        name: "iproute2"
        state: present
        update_cache: true
    - name: prepare | install dmidecode
      ansible.builtin.apt:
        name: "dmidecode"
        state: present
        update_cache: true
    - name: refetch facts
      setup:

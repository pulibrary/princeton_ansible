---
- name: Firewall | Allow variable defined rule on ufw
  community.general.ufw:
    rule: allow
    src: "{{ item }}"
    port: "{{ rule.port }}"
    proto: "{{ rule.protocol }}"
  loop: "{{ rule.allowed_cidrs }}"

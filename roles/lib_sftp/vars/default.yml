---
# vars/default.yml
# Perl modules for the Checkmk check that logs in over SFTP with a password.
# Debian names, which is the best guess for an unrecognised OS family.
lib_sftp_perl_packages:
  - cpanminus
  - libssh2-1-dev
lib_sftp_sshd_service: sshd

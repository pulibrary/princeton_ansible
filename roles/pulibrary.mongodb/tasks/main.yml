---
- name: add mongodb keys
  apt_key: url={{ url_apt_key }}0x7F0CEB10

- name: add repo sources
  apt_repository: repo='{{ mongodb_repository }}' update_cache=yes

- name: install mongo db
  apt: pkg={{ item }} state=installed
  with_items: 
    - mongodb-org

---
# defaults file for roles/pulibrary.lib-svn
svn_home: "/srv/svn/repos"
lib_svn_repo_dir: "{{ svn_home }}/libsvn"
harry_secret: "{{ vault_harry_secret | default('harrysecret') }}"
sally_secret: "{{ vault_sally_secret | default('harrysecret') }}"

---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: test for libsvn path
      stat:
        path: /srv/svn/repos/libsvn
      register: libsvn_path

    - name: assert the path existence
      assert:
        that:
          - libsvn_path.stat.exists

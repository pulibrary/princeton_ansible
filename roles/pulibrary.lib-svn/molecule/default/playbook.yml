---
- name: Converge
  hosts: all
  vars:
    - run_not_in_container: false
    - sallysecret: "{{ vault_sally_password | default('change_me') }}"
  vars_files:
    - ../../defaults/main.yml
    - ../../vars/testuser.yml
  roles:
    - role: pulibrary.lib-svn

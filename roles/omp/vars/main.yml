---
# vars file for roles/ojs
app_db_host: "{{ db_host | default('localhost') }}"
app_db_user: "{{ omp_db_user }}"
app_db_password: "{{ omp_db_password }}"
app_db_port: "{{ omp_db_port }}"

application_dbuser_name: "{{ omp_db_user }}"
application_dbuser_password: "{{ omp_db_password }}"
application_db_name: "{{ omp_db_name }}"
application_dbuser_role_attr_flag: "SUPERUSER"

---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: install ruby software
      apt:
        name: ["ruby-switch", "ruby2.4", "ruby2.4-dev"]
        state: present

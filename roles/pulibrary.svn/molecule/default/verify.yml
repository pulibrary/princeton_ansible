---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: install subversion
      apt:
        name: ["subversion"]
        state: present

    - name: check if connection still works
      ping:

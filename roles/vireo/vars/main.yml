---
# vars file for roles/vireo
app_db_host: "{{ db_host | default('localhost') }}"
app_db_user: "{{ vireo_db_user }}"
app_db_password: "{{ vireo_db_password }}"
app_db_port: "{{ vireo_db_port }}"

application_dbuser_name: "{{ vireo_db_user }}"
application_dbuser_password: "{{ vireo_db_password }}"
application_db_name: "{{ vireo_db_name }}"
application_dbuser_role_attr_flag: "SUPERUSER"

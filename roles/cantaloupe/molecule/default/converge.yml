---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
    - cantaloupe_user: deploy
    - cantaloupe_group: deploy
  become: true
  tasks:
    - name: "Include cantaloupe"
      include_role:
        name: cantaloupe

mariadb__server: "{{ db_host }}"

mariadb__users:
  - name: "{{ dss_db_user }}"
    host: "%"
    password: "{{ dss_db_password }}"
    priv: "{{ dss_db_name }}.*:ALL"

mariadb__databases:
  - name: "{{ dss_db_name }}"
    encoding: utf8mb4
    collation: utf8mb4_general_ci
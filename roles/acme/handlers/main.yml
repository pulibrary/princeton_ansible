---
# handlers file for roles/acme
- name: Run generate_certbot
  ansible.builtin.command: /tmp/generate_certbot.sh
  when: certbot_action == "generate"

- name: Run revoke_certbot
  ansible.builtin.command: /tmp/revoke_certbot.sh
  when: certbot_action == "revoke"

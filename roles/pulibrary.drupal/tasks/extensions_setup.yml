---
- name: enable mysql pdo extension
  apt:
    name: ["php7.2-gd", "php7.2-mysql"]
    state: present
    update_cache: true
  when: ansible_distribution_release == "xenial"
  notify: restart apache

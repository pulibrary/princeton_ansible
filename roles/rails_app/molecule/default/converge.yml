---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
    - passenger_listen_port: '8080'
    - node_identifier: 1
  tasks:
    - name: "Include rails_app"
      include_role:
        name: "rails_app"

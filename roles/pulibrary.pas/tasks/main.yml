---
- name: Install grunt-cli
  npm: name=grunt-cli global=yes
  become: true

- name: install sass
  gem:
    name: sass
    state: latest
    user_install: no

# Didn't clone https://github.com/pulibrary/pas.git
# - name: Install pas
#   git:
#     repo: git@github.com:pulibrary/pas.git
#     dest: /opt/pas

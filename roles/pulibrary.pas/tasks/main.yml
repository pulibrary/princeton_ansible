---
- name: Install grunt-cli
  npm: name=grunt-cli global=yes
  become: true

- name: install sass
  gem:
    name: sass
    state: latest
    user_install: no

- name: Install pas
  git:
    repo: https://github.com/pulibrary/pas.git
    dest: /opt/pas
  become: true

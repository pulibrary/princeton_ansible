---
- name: Converge
  hosts: all
  vars:
    running_on_server: false
  become: true
  pre_tasks:
    - name: Install GPG requirement for testing
      ansible.builtin.apt:
        name: gnupg
        state: present
  tasks:
    - name: "Include nodejs"
      ansible.builtin.include_role:
        name: nodejs

# tasks/packages.yml
---
- name: Bitcurator - update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: BitCurator - install packages
  ansible.builtin.apt:
    name: "{{ bitcurator_apt_packages }}"
    state: present

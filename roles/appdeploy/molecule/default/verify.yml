---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: Forever installed
    shell: 'forever --version | grep v4.0.1'
    register: result
    changed_when: result.rc != 0
    failed_when: False

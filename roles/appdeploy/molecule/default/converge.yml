---
- name: Converge
  hosts: all
  pre_tasks:
    - name: update cache
      apt:
        update_cache: true
        cache_valid_time: 600
  tasks:
    - name: "Include appdeploy"
      include_role:
        name: "appdeploy"
  vars:
    - running_on_server: false

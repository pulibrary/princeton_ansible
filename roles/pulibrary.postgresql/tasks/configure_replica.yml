---
- name: add postgres extra path
  lineinfile:
    dest: '/etc/environment'
    state: present
    backrefs: 'yes'
    regexp: 'PATH=(["]*)((?!.*?"{{ postgres_extra_path }}").*?)(["]*)$'
    line: 'PATH=\1\2:"{{ postgres_extra_path }}"\3'

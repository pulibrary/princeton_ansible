---
- name: PostgreSQL | Make sure the postgis extensions are installed
  apt:
    name: "{{ postgresql_ext_postgis_deps }}"
    state: present
    update_cache: true
    cache_valid_time: "{{ apt_cache_valid_time | default (3600) }}"
  notify:
    - restart postgres

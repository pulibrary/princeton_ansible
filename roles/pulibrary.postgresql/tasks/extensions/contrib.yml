---
- name: PostgreSQL |  Make sure the postgres contrib extensions are installed
  apt:
    name: "postgresql-contrib-{{ postgresql_version }}"
    state: present
    update_cache: true
    cache_valid_time: "{{ apt_cache_valid_time | default (3600) }}"
  notify:
    - restart postgres

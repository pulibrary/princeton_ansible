# PostgreSQL Client Authentication Configuration File
# ===================================================
#

# DO NOT DISABLE!
# If you change this first entry you will need to make sure that the
# database superuser can access the database using some other method.
# Noninteractive access to all databases is required during automatic
# maintenance (custom daily cronjobs, replication, and similar tasks).
#
# Database administrative login by Unix domain socket
local   all             postgres                                trust

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     md5
# IPv4 local connections:
host    all             all             127.0.0.1/32            md5
# IPv6 local connections:
host    all             all             ::1/128                 md5
# figgy connections
host    all             all             '{{ figgy_server0 }}'/32       md5
host    all             all             '{{ figgy_server1 }}'/32       md5
host    all             all             '{{ figgy_server2 }}'/32       md5
host    all             all             '{{ figgy_server3 }}'/32       md5
host    all             all             '{{ figgy_server4 }}'/32       md5
host    all             all             '{{ figgy_server5 }}'/32       md5
# DPUL connections
host    all             all             '{{ dpul_server0 }}'/32       md5
# orangelight connections
host    all             all             '{{ orangelight_server0 }}'/32       md5
host    all             all             '{{ orangelight_server1 }}'/32       md5
host    all             all             '{{ orangelight_server2 }}'/32       md5
host    all             all             '{{ orangelight_server3 }}'/32       md5
host    all             all             '{{ orangelight_server4 }}'/32       md5
host    all             all             '{{ orangelight_server5 }}'/32       md5
# catalog-staging connections
host    all             all             '{{ catalog-staging_server0 }}'/32       md5
# lae-staging connections
host    all             all             '{{ lae-staging_server0 }}'/32       md5

# Allow replication connections from localhost, by a user with the
# replication privilege.
#local   replication     postgres                                peer
#host    replication     postgres        127.0.0.1/32            md5
#host    replication     postgres        ::1/128                 md5
# replicated data
host    replication             replicant             '{{ replicant_server0 }}'/32       md5
# master
host    replication             replicant             '{{ replicant_server1 }}'/32       md5

---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: Podman installed
    shell: |
      set -o pipefail
      podman --version | grep "podman version 3"
    args:
      executable: /bin/bash
    register: result
    failed_when: result.rc != 0
    changed_when: false

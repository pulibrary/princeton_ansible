---
- name: ruby | check version
  ansible.builtin.command: ruby -v
  register: installed_ruby
  ignore_errors: true
  changed_when: false

- include_tasks: cleanup.yml
  when: installed_ruby.failed

- name: ruby | update rubygems
  ansible.builtin.command: gem update --system
  become: true

---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
  become: true
  pre_tasks:
    - name: update cache
      ansible.builtin.apt:
        update_cache: true
  tasks:
    - name: "Include ruby_s"
      ansible.builtin.include_role:
        name: ruby_s

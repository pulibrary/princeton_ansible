---
# vars file for roles/oar
app_db_host: "{{ db_host | default('localhost') }}"
app_db_user: "{{ dspace_db_user }}"
app_db_password: "{{ dspace_db_password }}"
app_db_port: "{{ dspace_db_port }}"

application_dbuser_name: "{{ dspace_db_user }}"
application_dbuser_password: "{{ dspace_db_password }}"
application_db_name: "{{ dspace_db_name }}"
application_dbuser_role_attr_flag: "SUPERUSER"

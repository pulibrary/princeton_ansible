---
- name: copy solr configuration
  template:
    src: solr.j2
    dest: /etc/monit/conf-available/solr

- name: create symbolic link
  file:
    src: /etc/monit/conf-available/solr
    dest: /etc/monit/conf-enabled/solr
    state: link
  notify:
    - restart monit

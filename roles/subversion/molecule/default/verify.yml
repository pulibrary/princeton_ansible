---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: check subversion package status
    package:
      name: subversion
      state: present
    check_mode: true
    register: pkg_status

  - name: test subversion package is installed
    assert:
      that:
        - not pkg_status.changed

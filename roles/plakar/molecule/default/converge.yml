---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
  become: true
  tasks:
    # Change this when copying to another role
    - name: "Include plakar"
      ansible.builtin.include_role:
        name: plakar

---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
  become: true
  pre_tasks:
    - name: update cache
      ansible.builtin.dnf:
        update_cache: true
  tasks:
    - name: "Include mflux"
      include_role:
        name: mflux

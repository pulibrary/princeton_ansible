---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: test for fits path
      stat:
        path: /opt/fits
      register: fits_path

    - name: assert the fits path existence
      assert:
        that:
          - fits_path.stat.exists

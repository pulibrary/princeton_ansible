---
# defaults file for system_ldap
ad_domain: "pu.win.princeton.edu"
ad_test_user: "{{ almasftp | default(omit) }}"
ad_domain_controller: "pu.win.princeton.edu"
ad_admin_user: "{{ doas-libsftp | default(omit) }}"

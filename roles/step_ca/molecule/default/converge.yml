---
- name: Converge
  hosts: all
  become: true
  vars:
    # Test the CA build path without contacting Entra.
    step_ca_password: "molecule-test-password"
    step_ca_oidc_enabled: false
    step_ca_manage_firewall: false
  roles:
    - role: step_ca

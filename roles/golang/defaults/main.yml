---
# defaults file for roles/golang
golang_version: "1.23.3"

# Architecture string for Go tarball. Override if needed. (for local molecule? building)
golang_arch: "amd64"

# Where to cache downloaded tarballs
golang_download_dir: "/usr/local/src"

# Where Go will be installed
golang_install_dir: "/usr/local/go"

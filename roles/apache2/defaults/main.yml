---
apache:
    docroot: '{{apache_app_path |default("/var/www/html")}}'
    servername: "{{ ansible_hostname }}"
    directory_options: ''
apache_listen_port: 80
datadog_trace_environment: ''
force_vhosts_update: false
---
galaxy_info:
  role_name: pas
  author: pulibrary
  description: Role to set up a machine for Princeton and Slavery
  company: Princeton University Library

  license: MIT

  min_ansible_version: 2.4.0.0

  platforms:
    - name: Ubuntu
      versions:
        - bionic

dependencies:
    # Will need to re-add common when we move it out of deploy_user and ruby_s
    # - role: common
  - role: deploy_user
  - role: php
  - role: mysql
  - role: samba
  # The ruby_s role may not be needed, but putting it here because 
  # we are removing it as a dependency of the Capistrano role
  - role: ruby_s
  - role: capistrano

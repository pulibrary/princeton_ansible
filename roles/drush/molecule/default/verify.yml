---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: verify drush path
    stat:
      path: "/usr/local/bin/drush"
    register: drush
    failed_when:
      - not drush.stat.exists

---
- name: Converge
  hosts: all
  become: true
  pre_tasks:
    - name: update cache
      apt:
        update_cache: true
        cache_valid_time: 600
  tasks:
    - name: "Include reservoir"
      include_role:
        name: reservoir

---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: vips version assertion
    command: vips --version
    register: cmd_result

  - name: assert vips version
    assert:
      that: "'vips-8.9.1' in cmd_result.stdout"

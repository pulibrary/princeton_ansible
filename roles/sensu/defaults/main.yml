---
# defaults file for sensu
# some of these settings are not needed for installing using the script
# but would be to install with Ansible tasks, as shown in the
# comments in the playbook
trusted_ca_file_path: /etc/sensu/sensu-agent-trusted-ca.crt
packagecloud_auth: ""
channel: stable
build: latest
os: ubuntu
ubuntu_release: bionic
dist: stable

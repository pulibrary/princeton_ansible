---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: test for cantaloupe path
      stat:
        path: /opt/cantaloupe
      register: cantaloupe_path

    - name: assert the existence of cantaloupe
      assert:
        that:
          - cantaloupe_path.stat.exists

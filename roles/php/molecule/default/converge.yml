---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
    - php_version: 8.1
  pre_tasks:
    - name: update cache
      apt:
        update_cache: true
        cache_valid_time: 600
  tasks:
    - name: "Include roles/php"
      include_role:
        name: "php"

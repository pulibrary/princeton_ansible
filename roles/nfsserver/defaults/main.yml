---
# defaults file for nfsserver
#
# add new server to share
#
# ```yaml
# shares:
#  - mount: "/var/nfs/bibdata"
#    name: "bibdata"
#    hosts:
#      - "{{ servers.bibdata.staging1 }}"
#      - "{{ servers.bibdata.staging2 }}"
#      - "{{ servers.bibdata.staging3 }}"  # New server added
# ```
# add new server to nfsserver_exports
#
# ```yaml
# nfsserver_exports:
#  - share: "{{ bibdata_fileshare_mount }}"
#    hosts:
#      - name: "{{ servers.bibdata.staging1 }}"
#        options: "{{ default_nfs_option }}"
#      - name: "{{ servers.bibdata.staging2 }}"
#        options: "{{ default_nfs_option }}"
#      - name: "{{ servers.bibdata.staging3 }}"  # New server added
#        options: "{{ default_nfs_option }}"
# ```


pul_shares: []
cdh_shares: []
nfsserver_cdh_shares: []
nfsserver_exports: []
nfsserver_pul_shares: []

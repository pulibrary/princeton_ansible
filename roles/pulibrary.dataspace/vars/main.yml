---
# vars file for roles/pulibrary.dataspace
postgres__databases:
  - name: dataspace_demo_db

postgres__users:
  - name: dataspace_demo_user
    password: "{{ vault_dataspace_demo_user_pass }}"
    db: dataspace_demo_db

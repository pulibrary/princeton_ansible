---
galaxy_info:
  role_name: oawaiver
  company: Princeton University Library
  description: OA Waiver
  author: pulibrary

  license: MIT

  min_ansible_version: "2.9"

  platforms:
    - name: Ubuntu
      versions:
        - bionic

dependencies:
    # Will need to re-add common when we move it out of deploy_user and ruby_s
    # - role: common
  - role: deploy_user
  - role: capistrano
  - role: rails_app
  - role: nodejs

# Ansible managed

server {
    listen 80;
    server_name lib-dbserver.princeton.edu;

    rewrite ^/visual_materials/maps(.*)$
    location / {
        return 301 https://library.princeton.edu$request_uri permanent;
    }
}

# Ansible managed
proxy_cache_path /data/nginx/prosody/NGINX_cache/ keys_zone=prosodycache:10m;

upstream prosody {
    zone prosody 64k;
    server cdh-prosody1.princeton.edu;
    sticky learn
          create=$upstream_cookie_prosodycookie
          lookup=$cookie_prosodycookie
          zone=prosodyclient_sessions:1m;
}

server {
    listen 80;
    server_name prosody.princeton.edu;

    location / {
        return 301 https://$server_name$request_uri;
    }
}

server {
    listen 80;
    server_name prosody-beta.princeton.edu;

    location / {
        return 301 https://prosody.princeton.edu$request_uri;
    }
}


server {
    listen 443 ssl;
    server_name prosody-beta.princeton.edu;

    ssl_certificate            /etc/nginx/conf.d/ssl/certs/cdh_prosody_princeton_edu_chained.pem;
    ssl_certificate_key        /etc/nginx/conf.d/ssl/private/cdh_prosody_princeton_edu_priv.key;
    ssl_session_cache          shared:SSL:1m;
    ssl_prefer_server_ciphers  on;

    location / {
        return 301 https://prosody.princeton.edu$request_uri;
        }
    }
    include /etc/nginx/conf.d/templates/prod-maintenance.conf;
}

server {
    listen 443 ssl;
    server_name prosody.princeton.edu;

    client_max_body_size 50M;

    ssl_certificate            /etc/nginx/conf.d/ssl/certs/cdh_prosody_princeton_edu_chained.pem;
    ssl_certificate_key        /etc/nginx/conf.d/ssl/private/cdh_prosody_princeton_edu_priv.key;
    ssl_session_cache          shared:SSL:1m;
    ssl_prefer_server_ciphers  on;

    location / {
#        app_protect_enable on;
        app_protect_security_log_enable on;
        proxy_pass http://prosody;
        proxy_set_header Host $http_host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache prosodycache;
        proxy_connect_timeout      2h;
        proxy_send_timeout         2h;
        proxy_read_timeout         2h;
    }
    include /etc/nginx/conf.d/templates/prod-maintenance.conf;
}

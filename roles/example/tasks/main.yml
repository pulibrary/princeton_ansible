---
# roles/example

- name: Example | ensure example package is present
  ansible.builtin.package:
    name: curl
    state: present

- name: Example | example service unit directory exists
  ansible.builtin.file:
    path: /etc/systemd/system/example.service.d
    state: directory
    mode: "0755"

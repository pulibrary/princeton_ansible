---
- name: Verify
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: test for file path
      stat:
        path: /etc/environment
      register: path

    - name: assert the existence
      assert:
        that:
          - path.stat.exists

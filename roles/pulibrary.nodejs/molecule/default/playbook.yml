---
- name: Converge (install a NodeJS 12 release)
  hosts: all
  roles:
    - role: pulibrary.nodejs
      vars:
        nodejs__upstream_release: 'node_12.x'
        nodejs__upstream_key_id: '68576280'

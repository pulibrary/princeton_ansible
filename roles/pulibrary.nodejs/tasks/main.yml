---
- name: Get upstream APT GPG key
  apt_key:
    id: "{{ nodejs__upstream_key_id }}"
    keyserver: "hkp://pool.sks-keyservers.net"
    state: present
  ignore_errors: true

- name: Add upstream Yarn APT repository GPG key from MIT
  apt_key:
    id: "{{ nodejs__yarn_upstream_key_id }}"
    state: present
    keyserver: "hkp://pgp.mit.edu"

- name: Configure upstream APT repository
  apt_repository:
    repo: "{{ nodejs__upstream_repository }}"
    state: present
    update_cache: true

- name: Add upstream Yarn APT repository GPG key
  apt_key:
    id: "{{ nodejs__yarn_upstream_key_id }}"
    state: present
    keyserver: "hkp://pool.sks-keyservers.net"
  ignore_errors: true

- name: Add upstream Yarn APT repository GPG key from MIT
  apt_key:
    id: "{{ nodejs__yarn_upstream_key_id }}"
    state: present
    keyserver: "hkp://pgp.mit.edu"

- name: Add upstream Yarn APT repository
  apt_repository:
    repo: "{{ nodejs__yarn_upstream_repository }}"
    state: present
    update_cache: true

- name: Install node packages
  apt:
    name: ["nodejs", "yarn"]
    state: present
    autoremove: true

---
# vars file for roles/pulibrary.elucidate
app_db_host: "{{ db_host | default('localhost') }}"
app_db_user: "{{ elucidate_db_user }}"
app_db_password: "{{ elucidate_db_password }}"
app_db_port: "{{ elucidate_db_port }}"

application_dbuser_name: "{{ elucidate_db_user }}"
application_dbuser_password: "{{ elucidate_db_password }}"
application_db_name: "{{ elucidate_db_name }}"
application_dbuser_role_attr_flag: "SUPERUSER"

---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Bootstrap python and basic packages
      ansible.builtin.raw: |
        apt update && 
        apt install -y python3 python3-apt logrotate rsyslog vim tmux curl wget htop
      register: bootstrap
      changed_when: bootstrap.rc == 0

    - name: Gather facts
      ansible.builtin.setup:


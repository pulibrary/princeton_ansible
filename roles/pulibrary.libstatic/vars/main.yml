---
# vars file for roles/pulibrary.libstatic
apache_app_path: '/var/local/firestone-renovations'
apache:
    docroot: '{{ apache_app_path }}'
    servername: '{{ ansible_hostname }}'
    directory_options: ''
---
- name: Zookeeper_src | configuring service
  ansible.builtin.service:
    name: zookeeper
    state: "{{ zookeeper_service_state }}"
    enabled: "{{ zookeeper_service_enabled }}"
  when:
    - running_on_server
  changed_when: false

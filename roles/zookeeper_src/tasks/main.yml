---
# tasks file for zookeeper_src
- name: Zookeeper_src | install steps
  ansible.builtin.include_tasks: install.yml
  when:
    - running_on_server

- name: Zookeeper_src | configuration steps
  ansible.builtin.include_tasks: config.yml
  when:
    - running_on_server

- name: Zookeeper_src | set up service
  ansible.builtin.include_tasks: service.yml
  when:
    - running_on_server

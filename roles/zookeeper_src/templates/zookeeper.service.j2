# {{ansible_managed}}

[Unit]
Description=ZooKeeper

[Service]
Type=simple
User={{ zookeeper_user }}
Group={{ zookeeper_group }}
ExecStart={{ install_dir }}/apache-zookeeper-{{ zookeeper-version }}/bin/zkServer.sh start-foreground

TimeoutSec=300

[Install]
WantedBy=multi-user.target

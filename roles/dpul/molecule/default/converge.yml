---
- name: Converge
  hosts: all
  vars:
    - running_on_server: false
    - deploy_user: deploy
  become: true
  pre_tasks:
    - name: Update cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 600
  tasks:
    - name: "Include dpul"
      ansible.builtin.include_role:
        name: dpul

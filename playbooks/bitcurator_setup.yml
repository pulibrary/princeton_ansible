---
- name: BitCurator Environment Setup
  hosts: sandbox-bitcur1.princeton.edu
  # hosts: "{{ bitcurator_production }}" when approved
  remote_user: pulsys
  become: true
  vars_files:
    - ../group_vars/bitcurator/shared.yml
    - ../group_vars/bitcurator/vault.yml

  roles: 
    - bitcurator


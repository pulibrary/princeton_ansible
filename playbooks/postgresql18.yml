---
# by default this playbook runs in the staging environment
# to run in qa, pass '-e runtime_env=qa'
# to run in production, pass '-e runtime_env=production'
- name: Build postgresql 18 project
  hosts: postgresql_{{ runtime_env | default('staging') }}
  remote_user: pulsys
  become: true
  vars_files:
    - ../group_vars/postgresql18/{{ runtime_env | default('staging') }}.yml
    - ../group_vars/postgresql/vault.yml
  roles:
    - role: roles/common
    - role: roles/otel_collector
    - role: roles/beyla
    - role: ../roles/postgresql

  post_tasks:
    - name: tell everyone on slack you ran an ansible playbook
      community.general.slack:
        token: "{{ vault_pul_slack_token }}"
        msg: "Ansible ran `{{ ansible_play_name }}` on {{ inventory_hostname }}"
        channel: "{{ slack_alerts_channel }}"

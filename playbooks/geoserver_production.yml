---
# when running this playbook make sure you use the 
# `-e deploy_user_id=1002` flag
- name: build or update geoserver system
  hosts: geoserver
  remote_user: pulsys
  become: true
  vars_files:
    - ../site_vars.yml
    - ../group_vars/geoserver/production.yml
    - ../group_vars/geoserver/vault.yml
  roles:
    - role: roles/tomcat8
    - role: roles/deploy_user
    - role: roles/postgresql
    - role: roles/samba
    - role: roles/geoserver

  post_tasks:
    - name: tell everyone on slack you ran an ansible playbook
      slack:
        token: "{{ vault_pul_slack_token }}"
        msg: "Ansible ran `{{ ansible_play_name }}` on {{ inventory_hostname }}"
        channel: "{{ slack_alerts_channel }}"

---
dist: xenial
sudo: required
language: python
python:
  - "2.7"
  - "3.7"
services: docker

env:
  matrix:
    - MOLECULE_DISTRO: ubuntu1604
    - MOLECULE_DISTRO: ubuntu1804

script:
  # Run tests
  - molecule test

---
nomad_volumes:
  # Docker runs with userns-remap (dockremap:1000000), so the host uid/gid is
  # the in-container zookeeper user (1000) plus that offset.
  - name: 'zookeeper-sandbox'
    count: 3
    node_pool: 'sandbox'
    uid: 1001000
    gid: 1001000

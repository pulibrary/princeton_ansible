---
# vault password entered using `python -c 'import crypt; print(crypt.crypt("PLAINTEXT_PASSWORD", crypt.mksalt(crypt.METHOD_SHA512)))'`
backup_users:
  - name: bkppalofw
    password: "{{ vault_bkppalofw_pass }}"
  - name: bkpvcenter
    password: "{{ vault_bkpvcenter_pass }}"

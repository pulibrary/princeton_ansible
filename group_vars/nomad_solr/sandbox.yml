---
nomad_volumes:
  # Docker runs with userns-remap (dockremap:1000000), so the host uid/gid is
  # the in-container solr user (8983) plus that offset.
  - name: 'solr-sandbox'
    count: 3
    node_pool: 'sandbox'
    uid: 1008983
    gid: 1008983

---
aws_project_user_password: "{{ vault_project_user_password }}"
s3_bucket: "pul-cloud-staging"
s3_image_bucket: "pul-image-staging"
stack_name: pul-cloud-staging
source_bucket: "{{ s3_image_bucket }}"
cache_domain_name: "pul-cloud-staging.princeton.edu"
aws_region: "us-east-1"
serverless_path: "{{ iiif_path | default('~/serverless-iiif') }}"
project_user: "{{ pul_group }}"
dev_user: "{{ dev_user }}"
project_user_password: "{{ aws_project_user_password }}"
project_user_aws_access_key: "{{ vault_project_user_aws_access_key }}"
project_user_aws_secret_key: "{{ vault_project_user_aws_secret_key }}"
lambda_dir: "{{ serverless_path }}/serverless-iiif/sam/cloudfront"
iiif_domain: "puliiif"

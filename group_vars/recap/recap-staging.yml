---
recap_base_url: 'recap-staging.princeton.edu'
drupal_ssl_base_path: 'https://{{ recap_base_url }}'

drupal_db_user: 'recap_staging'
drupal_db_password: "{{ recap_db_password }}"
drupal_db_name: 'recap_staging'
drupal_db_port: '5432'
drupal_docroot: '/var/www/recap_cap'
apache_app_path: '{{ drupal_docroot }}/current'

### Uncomment this to force a dump file to be imported
# drupal_dbimport_file: 'dump.sql'

db_host: 'lib-postgres-staging1.princeton.edu'
db_password: "{{ vault_replicant_password }}"
db_user: "replicant"

postgres_host: "{{ db_host }}"
postgres_port: "{{ drupal_db_port }}"
postgres_admin_user: "{{ db_user }}"
postgres_admin_password: "{{ db_password }}"
postgresql_is_local: false
postgres_version: 12
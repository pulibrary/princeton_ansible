---
mysql_server: false

mysql_host: "mysql-db-staging1.princeton.edu"

mysql_root_password: "{{ vault_maria_mysql_root_password }}"
mysql_databases:
  - name: "video_reserves_staging_db"
    encoding: utf8mb4
    collation: utf8mb4_general_ci

mysql_users:
  - name: "video_reserves_staging_db_user"
    host: "%"
    password: "{{ vault_video_reserves_staging_user_password }}"
    priv: "video_reserves_staging_db.*:ALL"

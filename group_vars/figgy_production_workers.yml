---
figgy_tags: "application:figgy, environment:production, type:worker"
datadog_checks:
  nginx:
    '{{figgy_datadog_nginx_check}}'
  ruby:
    '{{figgy_datadog_ruby_check}}'
  http_check:
    '{{figgy_datadog_http_check}}'

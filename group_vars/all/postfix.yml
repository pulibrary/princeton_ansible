---
relay_host: true
smtp_relay_host: "{{ proofpoint_host }}"
smtp_relay_user: "{{ proofpoint_username }}"
smtp_relay_password: "{{ proofpoint_password }}"
smtp_relay_port: "{{ proofpoint_port }}"
proofpoint_username: "{{ vault_proofpoint_username }}"
proofpoint_password: "{{ vault_proofpoint_password }}"
proofpoint_host: "{{ vault_proofpoint_host }}"
proofpoint_port: "587"


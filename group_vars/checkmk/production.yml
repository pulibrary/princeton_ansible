---
# Agent role
# what is 'agent_prep_legacy' for?
# we only set it in production
checkmk_agent_prep_legacy: 'false'
checkmk_agent_server: "pulmonitor-staging1.princeton.edu"
checkmk_agent_site: production
# TODO:
# the 'automation' user exists
# and we reference it in the 'shared.yml' vars file:
  # checkmk_agent_user: automation
# we need to reset its secret on the production site and
# add that value to the 'vault.yml' vars file so we can use it like this:
  # checkmk_agent_secret: "{{ vault_automation_secret_production }}"
#
# in the agent_common.yml file we had:
  # checkmk_agent_secret: "{{ vault_ansible_automation_secret }}"
# and in the remote.yml file we had:
  # checkmk_agent_secret: "{{ vault_automation_secret }}"


# checkmk_folders:
#   - path: /linux/sandboxes
#     title: Sandboxes

# Server Role

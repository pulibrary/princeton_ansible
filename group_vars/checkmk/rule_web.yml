checkmk_rules:
  - description: "Adding cert validation for pdc-describe"
    name: "Check PDC-Describe Certificates"
    comment: "Make sure our SSL certificates are valid"
    value_raw: "{'host': {'address': ('direct', 'pdc-describe-staging.princeton.edu')},\n 'mode': ('cert', {'cert_days': (60, 10)}),\n 'name': 'SSL Check'}"
    ruleset: "active_checks:http"
    folder: "/"

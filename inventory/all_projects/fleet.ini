# Fleet's database server runs Oracle MySQL, not MariaDB, so it is kept out of
# the mysql_* groups: the mysql playbook and role would replace MySQL with
# MariaDB on this host.
[fleet_mysql_production]
fleet-mysql-prod1.lib.princeton.edu
